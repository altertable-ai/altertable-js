{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "clean": "lerna run clean",
    "lint": "lerna run lint",
    "test": "lerna run test",
    "test:watch": "lerna run test:watch",
    "bump": "bun run scripts/bump-versions.ts"
  },
  "dependencies": {},
  "devDependencies": {
    "@types/bun": "^1.2.17",
    "lerna": "^8.2.2"
  },
  "resolutions": {
    "esbuild": "^0.25.4",
    "brace-expansion": "^2.0.2"
  }
}
